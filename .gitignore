# Dependencies
node_modules/

# Freebuff
.freebuff/

# OS
.DS_Store
Thumbs.db
nul

# Capacitor / Android
android/
ios/
dist/
build/
out/

# Secrets / local overrides
.env
.env.local
# The Android upload keystore and its credentials — never commit these. Losing the
# keystore means losing the ability to update the app on Play (the upload key can be
# reset with Google, but only if you can prove ownership).
keystore/
*.jks
*.keystore

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Service worker cache (generated)
# (keep service-worker.js committed — it's source, not generated)
design-system/
